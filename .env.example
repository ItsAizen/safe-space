# Vercel KV Configuration
# Get these from Vercel Dashboard > Storage > KV > .env.local
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: For local development without Vercel KV
# The app will automatically fall back to localStorage if these are not set